# INBOX9 service icon assets

Reserved for the Phase 2 service PNGs.

Use the stable service ID as the filename:

`svc-<slug>.png`

The filename must match the `id` returned by `GET /api/services`. This keeps logo lookup independent of display-name casing and future catalog reordering.